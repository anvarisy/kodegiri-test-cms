# kodegiri-test-cms
Run frontend
```
cd frontend
npm install
npm start
```

default username && password in DB
```
username: kodegiri
password: kodegiri
```

Database postgres configuration
```
database_name = kodegiri
username = postgres
password = postgres
```
